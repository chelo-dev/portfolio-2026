---
---

<header class="brand-header">
  <h1 class="brand-name">ANGEL PAREDES TORRES</h1>
  <!-- <p class="brand-subtitle">Full Stack Developer</p> -->
</header>

<style>
  .brand-header {
    text-align: center;
    padding: 3rem 2rem 2rem 2rem;
    position: relative;
    z-index: 10;
  }

  .brand-name {
    font-size: 4rem;
    font-weight: 900;
    letter-spacing: 0.3rem;
    margin-bottom: 0.5rem;
    background: linear-gradient(135deg, #ffffff 0%, #5c9dad 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-shadow: 0 4px 20px rgba(92, 157, 173, 0.3);
  }

  .brand-subtitle {
    font-size: 1.3rem;
    letter-spacing: 0.4rem;
    font-weight: 300;
    color: #a8b2d1;
    text-transform: uppercase;
  }

  @media (max-width: 768px) {
    .brand-name {
      font-size: 2.5rem;
      letter-spacing: 0.15rem;
    }

    .brand-subtitle {
      font-size: 1rem;
      letter-spacing: 0.2rem;
    }
  }
</style>
<div class="profile-container">
  <div class="profile-wrapper">
    
    <div class="profile-hexagon">
      <div class="hexagon-inner">
        <img src="/images/profile/angel.webp" alt="Angel Paredes Torres" class="profile-image" />
      </div>
    </div>

    <div class="geometric-lines">
      <svg viewBox="0 0 200 200" class="line-svg">
        <path d="M 10,10 L 190,10" class="line line-1" />
        <path d="M 190,10 L 190,190" class="line line-2" />
        <path d="M 190,190 L 10,190" class="line line-3" />
        <path d="M 10,190 L 10,10" class="line line-4" />
        <circle cx="100" cy="100" r="80" class="line line-5" />
      </svg>
    </div>

    <div class="status-indicator">
      <div class="status-dot"></div>
      <span class="status-text">Disponible</span>
    </div>
  </div>
</div>

<style>
  .profile-container {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 2rem;
    position: relative;
  }

  .profile-wrapper {
    position: relative;
    width: 300px;
    height: 300px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  /* Hexágono con clip-path */
  .profile-hexagon {
    position: relative;
    width: 250px;
    height: 250px;
    filter: drop-shadow(0 20px 40px rgba(92, 157, 173, 0.4));
    animation: float 6s ease-in-out infinite;
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0) rotate(0deg);
    }
    50% {
      transform: translateY(-10px) rotate(2deg);
    }
  }

  .hexagon-inner {
    width: 100%;
    height: 100%;
    position: relative;
    clip-path: polygon(
      25% 5%, 
      75% 5%, 
      100% 50%, 
      75% 95%, 
      25% 95%, 
      0% 50%
    );
    background: linear-gradient(135deg, #5c9dad 0%, #2b2d42 100%);
    padding: 4px;
  }

  .hexagon-inner::before {
    content: '';
    position: absolute;
    inset: 4px;
    background: #1a1b2e;
    clip-path: polygon(
      25% 5%, 
      75% 5%, 
      100% 50%, 
      75% 95%, 
      25% 95%, 
      0% 50%
    );
  }

  .profile-image {
    position: absolute;
    inset: 8px;
    width: calc(100% - 16px);
    height: calc(100% - 16px);
    object-fit: cover;
    clip-path: polygon(
      25% 5%, 
      75% 5%, 
      100% 50%, 
      75% 95%, 
      25% 95%, 
      0% 50%
    );
    transition: transform 0.3s ease;
  }

  .profile-hexagon:hover .profile-image {
    transform: scale(1.05);
  }

  /* Tech Badges flotantes */
  .tech-badge {
    position: absolute;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(92, 157, 173, 0.9);
    backdrop-filter: blur(10px);
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-size: 0.85rem;
    font-weight: 600;
    color: white;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
    animation: badge-float 3s ease-in-out infinite;
  }

  .badge-icon {
    font-size: 1.2rem;
  }

  .badge-1 {
    top: -10px;
    left: -30px;
    animation-delay: 0s;
  }

  .badge-2 {
    top: 50%;
    right: -40px;
    transform: translateY(-50%);
    animation-delay: 1s;
  }

  .badge-3 {
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    animation-delay: 2s;
  }

  @keyframes badge-float {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-8px);
    }
  }

  .badge-2 {
    animation-name: badge-float-horizontal;
  }

  @keyframes badge-float-horizontal {
    0%, 100% {
      transform: translateY(-50%) translateX(0);
    }
    50% {
      transform: translateY(-50%) translateX(-5px);
    }
  }

  .badge-3 {
    animation-name: badge-float-center;
  }

  @keyframes badge-float-center {
    0%, 100% {
      transform: translateX(-50%) translateY(0);
    }
    50% {
      transform: translateX(-50%) translateY(-8px);
    }
  }

  /* Líneas geométricas decorativas */
  .geometric-lines {
    position: absolute;
    inset: -20px;
    pointer-events: none;
    opacity: 0.3;
  }

  .line-svg {
    width: 100%;
    height: 100%;
  }

  .line {
    fill: none;
    stroke: #5c9dad;
    stroke-width: 1;
    stroke-dasharray: 5, 5;
  }

  .line-1 {
    animation: dash 20s linear infinite;
  }

  .line-2 {
    animation: dash 20s linear infinite 5s;
  }

  .line-3 {
    animation: dash 20s linear infinite 10s;
  }

  .line-4 {
    animation: dash 20s linear infinite 15s;
  }

  .line-5 {
    stroke-dasharray: 10, 10;
    animation: dash-reverse 30s linear infinite;
  }

  @keyframes dash {
    to {
      stroke-dashoffset: -100;
    }
  }

  @keyframes dash-reverse {
    to {
      stroke-dashoffset: 100;
    }
  }

  /* Indicador de estado */
  .status-indicator {
    position: absolute;
    bottom: -40px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(10px);
    padding: 0.6rem 1.2rem;
    border-radius: 25px;
    border: 1px solid rgba(92, 157, 173, 0.3);
    white-space: nowrap;
  }

  .status-dot {
    width: 10px;
    height: 10px;
    background: #00ff88;
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
    box-shadow: 0 0 10px #00ff88;
  }

  @keyframes pulse {
    0%, 100% {
      opacity: 1;
      transform: scale(1);
    }
    50% {
      opacity: 0.6;
      transform: scale(1.2);
    }
  }

  .status-text {
    font-size: 0.85rem;
    font-weight: 500;
    color: #a8b2d1;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .profile-wrapper {
      width: 250px;
      height: 250px;
    }

    .profile-hexagon {
      width: 200px;
      height: 200px;
    }

    .tech-badge {
      font-size: 0.75rem;
      padding: 0.4rem 0.8rem;
    }

    .badge-text {
      display: none;
    }

    .badge-1 {
      left: -10px;
    }

    .badge-2 {
      right: -10px;
    }

    .status-indicator {
      bottom: -35px;
      font-size: 0.8rem;
      padding: 0.5rem 1rem;
    }
  }

  @media (max-width: 480px) {
    .profile-wrapper {
      width: 200px;
      height: 200px;
    }

    .profile-hexagon {
      width: 180px;
      height: 180px;
    }

    .geometric-lines {
      display: none;
    }
  }
</style>